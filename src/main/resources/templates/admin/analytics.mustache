<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Analytics - URL Shortener</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <span class="navbar-brand">URL Shortener Admin</span>
            <div class="navbar-nav ms-auto">
                <span class="nav-item nav-link">Welcome, {{username}}</span>
                <a class="nav-link" href="/">Home</a>
                <a class="nav-link" href="/logout">Logout</a>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <div class="row">
            <div class="col-12">
                <div class="d-flex justify-content-between align-items-center mb-4">
                    <h1>Admin Analytics</h1>
                    <button class="btn btn-primary" onclick="location.reload()">Refresh</button>
                </div>

                <!-- Summary Card -->
                <div class="card mb-4">
                    <div class="card-header bg-primary text-white">
                        <h5 class="mb-0">Summary</h5>
                    </div>
                    <div class="card-body">
                        <div class="row">
                            <div class="col-md-4">
                                <div class="text-center">
                                    <h3 class="text-primary">{{totalUrls}}</h3>
                                    <p class="mb-0">Total URLs</p>
                                </div>
                            </div>
                            <div class="col-md-4">
                                <div class="text-center">
                                    <h3 class="text-info">{{totalAccessCount}}</h3>
                                    <p class="mb-0">Total Access Count</p>
                                </div>
                            </div>
                            <div class="col-md-4">
                                <div class="text-center">
                                    <h3 class="text-success">Active</h3>
                                    <p class="mb-0">System Status</p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- URL Statistics -->
                <div class="card">
                    <div class="card-header bg-secondary text-white">
                        <h5 class="mb-0">URL Statistics</h5>
                    </div>
                    <div class="card-body">
                        {{#hasUrls}}
                        <div class="table-responsive">
                            <table class="table table-striped table-hover">
                                <thead class="table-dark">
                                    <tr>
                                        <th>Short Code</th>
                                        <th>Original URL</th>
                                        <th>Access Count</th>
                                        <th>Created</th>
                                        <th>Last Accessed</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    {{#urlStatistics}}
                                    <tr>
                                        <td>
                                            <code>{{shortenedUrl}}</code>
                                        </td>
                                        <td>
                                            <a href="{{originalUrl}}" target="_blank" class="text-truncate d-inline-block" style="max-width: 300px;">
                                                {{originalUrl}}
                                            </a>
                                        </td>
                                        <td>
                                            <span class="badge bg-primary">
                                                {{accessCount}}
                                            </span>
                                        </td>
                                        <td>
                                            <small class="text-muted">{{createdAt}}</small>
                                        </td>
                                        <td>
                                            <small class="text-muted">{{lastAccessed}}</small>
                                        </td>
                                    </tr>
                                    {{/urlStatistics}}
                                </tbody>
                            </table>
                        </div>
                        {{/hasUrls}}
                        
                        {{^hasUrls}}
                        <div class="text-center py-5">
                            <h4 class="text-muted">No URLs Created Yet</h4>
                            <p class="text-muted">Once users start creating shortened URLs, they will appear here with their analytics.</p>
                            <a href="/" class="btn btn-primary">Go to URL Shortener</a>
                        </div>
                        {{/hasUrls}}
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Auto-refresh every 30 seconds
        setTimeout(function() {
            location.reload();
        }, 30000);
    </script>
</body>
</html>